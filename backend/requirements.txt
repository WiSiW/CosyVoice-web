# ---------------------------------------------------------------------------
# Web 服务层依赖（必需）
# ---------------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.30.0
python-multipart==0.0.20
pydantic==2.7.0
pydantic-settings==2.6.1
numpy==1.26.4
soundfile==0.12.1
python-dotenv==1.0.1

# ---------------------------------------------------------------------------
# 测试依赖
# ---------------------------------------------------------------------------
pytest==8.3.3
httpx==0.27.2

# ---------------------------------------------------------------------------
# CosyVoice 推理依赖（体积较大，真实推理必需，见 requirements-cosyvoice.txt）
# ---------------------------------------------------------------------------
# 推荐直接安装官方依赖，避免版本漂移：
#   pip install -r ../CosyVoice/requirements.txt
#
# 若 CosyVoice 仓库尚未克隆，可取消下面注释按需安装：
# torch==2.3.1
# torchaudio==2.3.1
# modelscope==1.20.0
# HyperPyYAML==1.2.3
# onnxruntime==1.18.0
# conformer==0.3.2
# transformers==4.51.3
